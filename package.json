{
  "name": "@hideya/langchain-google-genai-ex",
  "version": "0.0.0",
  "description": "ChatGoogleGenerativeAI extension that fixes schema compatibility issues with MCP tools and Gemini API",
  "keywords": [
    "langchain",
    "google",
    "gemini",
    "model context protocol",
    "mcp",
    "tools",
    "function calling",
    "schema",
    "llm",
    "generative-ai",
    "ai"
  ],
  "author": "Hideya",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hideya/langchain-google-genai-ex.git"
  },
  "bugs": {
    "url": "https://github.com/hideya/langchain-google-genai-ex/issues"
  },
  "homepage": "https://github.com/hideya/langchain-google-genai-ex#readme",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "build:watch": "tsup src/index.ts --format cjs,esm --dts --watch",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "node --test dist/test/*.js",
    "test:simple": "tsx src/test/simple.test.ts",
    "test:integration": "tsx src/test/integration.test.ts",
    "test:individual-servers": "tsx src/test/individual-servers.test.ts",
    "test:negative": "tsx src/test/negative.test.ts",
    "test:slim": "tsx src/test/slim.test.ts",
    "test:upstream-approach-failure": "tsx src/test/upstream-approach-failure.test.ts",
    "test:tool-contamination-effect": "tsx src/test/tool-contamination-effect.test.ts",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "clean:all": "git clean -fdxn -e .env && echo 'Files to be deleted ^. Continue? (Ctrl+C to cancel)' && read -p 'Press Enter to continue...' && git clean -fdx -e .env",
    "publish:test": "npm run clean:all && npm install && npm run build && npm publish --access=public --dry-run",
    "publish:do": "npm run clean:all && npm install && npm run build && npm publish --access=public",
    "docs:build": "npx typedoc --options typedoc.json",
    "docs:deploy": "npm run docs:build && ghp-import -n -p -f docs",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "~0.21.0",
    "@langchain/google-genai": "~0.2.16"
  },
  "peerDependencies": {
    "@langchain/core": ">=0.3.0"
  },
  "devDependencies": {
    "@langchain/core": "^0.3.0",
    "@langchain/langgraph": "^0.2.0",
    "@langchain/mcp-adapters": "^0.6.0",
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "dotenv": "^16.4.0",
    "eslint": "^9.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "typedoc": "^0.26.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
